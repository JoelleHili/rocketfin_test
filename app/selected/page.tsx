'use client';

import { SelectedTypes } from "./types/selectedTypes";

export default function Selected({ symbol }: SelectedTypes) {

    console.log("Selected Company:", symbol)

    return (
        <main>
        </main>
    );
}